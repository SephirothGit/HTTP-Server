package main

import (
	"context"
	"errors"
)
/*
type OrderService interface {
	UpdateStatus(ctx context.Context, id string, status string) error
}

var (
	ErrOrderNotFound = errors.New("order not found")
	ErrInvalidTransition = errors.New("ivalid status transition")
)*/
